一.回溯可解类题：

回溯三要素：
(1) 有效的结果(停止的条件)
(2) 回溯的条件
(3) 剪枝的条件

1. Q17 LetterCombinations
2. Q46 Permutation
3. Q47 PermuteUnique 2
4. Q79 WordSearch
5. Q78 Subset
6. Q90 Subset 2
7. Q37 SolveSudoku
8. Q77 K-Permutation

二. K数之和：
(1) 基本思路排序后从0 ~ len-1固定一个数值，从剩下的最左和最右进行夹逼
(2) 当当前基准元素为正数时，后面所有元素都为正数，即可跳出
(3) 向前去重(由于两个元素相同这种情况以及被计算过)，不能向后去重，因为有同一元素出现两次-1,-1,2的这种情况
(4) 需要下标合法才能进行计算，不能重叠
(5) 合法情况下，向后去重

1. Q1 两数之和
2. Q15 三数之和  (代码架构)
3. Q16 最接近的三数之和
4. Q18 四数之和

三. 股票类问题
1.定义动态规划问题 dp[i][j][s]表示第i天，至今最多允许交易j次的情况下，s状态下的利润
 0 <= i < len, 1 <= j <= K, s = {0, 1}, 0表示没持有，1表示持有

2. 状态转移方程
dp[i][k][0], 第i天空仓的收益
(1) 第i-1天空仓,                dp[i-1][k][0]
(2) 第i-1天持仓，第i天卖了       dp[i-1][k][1]+prices[i]
dp[i][k][1]，第i天持仓的收益
(1) 第i-1天持仓,                dp[i-1][k][1]
(2) 第i-1天空仓，第i天买了       dp[i-1][k-1][0]-prices[i]

3.边界条件
dp[-1][k][0] = 0
dp[-1][k][1] = -inf
dp[i][0][0] = 0
dp[i][0][1] = -inf

四. 贪心算法类问题
1. Q435 无重叠区间
2. Q452 最少的箭头射爆气球
3. Q55 跳跃游戏
4. Q45 跳跃游戏II
5. Q1024 视频拼接

五. 字典序类题目
1. Q31 下一个排列
2. Q60 第k个排列

六. 单调栈类题目
1. Q42 接雨水
2. Q84 柱状图中的最大矩形面积
3. Q85 最大矩形面积
4. Q739 每日温度
5. Q496 下一个更大的元素I
6. Q503 下一个更大的元素II
7. Q901 股票价格跨度
8. Q402 删掉K位数字


MS 高频LeetCode面试题：
1. Q1246 删除回文子数组，区间dp
2. Q300 最长上升子序列长度，Q673 最长上升子序列个数
3. Q239 滑动窗口最大值
4. Q94 二叉树的非递归中序遍历
5. Q21,23 合并多条有序链表
6. Q78 集合的所有子集
7. Q114, 426 二叉树转双向链表
8. Q42 接雨水
9. Q22 括号生成   ---------- recently
10. Q10 正则表达式 Q44 通配符匹配
11. Q42 接雨水，Q84 柱状图中的最大矩形，Q85 最大矩形
12. Q53 最大子序和
13. Q120 三角形最小路径和题解
14. Q124 二叉树中的最大路径和
15. Q450 删除二差搜索树中的节点
16. Q5 最长回文子串
17. Q510 中序遍历的下一个子节点
18. 单链表逆序
    二进制转十进制，考虑溢出
Q583 二叉树：当前节点的值更新为当前节点所有子节点的值之和（递归和非递归）

MS 高频面试题：
1. 判断一个数组是否几乎有序，「几乎有序」指两种情况: 1.交换两个元素后有序 2. 交换一个子串后有序 。
此外要求O(1)空间。

2. 输入: n个事件或查询的流(即在线)。 1.每个事件包含发生时间、事件种类。 2. 每个查询包含开始时间、结束时间、一个整数k。
   输出: 每个查询返回该时间段内发生总次数前k多的事件种类

3. 输入: 一个任意的链表，元素都是整数。
   输出: 求重复数有几种类。
   样例: 对[3->1->4->1->5->3]输出2。
   要求在空间复杂度最小的情况下优化时间。

4. 有n个二维平面上的点和距离d，将它们分成k组。
   若两点间的距离小于等于d，则这两个点在一组；如果点A和点B在一组，点B和点C在一组，则点A和点C在一组。
   算法返回每个点所在组的编号(0..k-1)

5. 长为N的数组中，每个值都是1-N范围内的，现在有某个数字出现了两次，某个数字丢失了，其他的数字仅出现一次，求重复的数和丢失的数。